# Mototelos — agent notes

## Canonical clone

The machine you actually run is the **canonical clone**:

`D:\repositories\motorcycle_dynamics_v4`

GitHub is backup. Grok sessions often work in a separate worktree. Local Hoster always starts the D:\ clone.

## Bake

**Euphemism:** `bake it` / `bake this` / `ok bake this` / `/bake`

Means: pull all updates into the canonical clone on `D:\repositories\motorcycle_dynamics_v4`.

When the user says that, follow `.grok/skills/bake/SKILL.md`. First line of the reply: **Ok pulling all updates into the canonical clone...** Confirm with the new HEAD when done.
